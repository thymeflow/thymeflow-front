{{#if queries.length}}
  <table class="sparql-history-table">
    {{#each queries as |query|}}
      {{sparql-history-grid-item query=query openQuery=(action "openQuery")}}
    {{/each}}
  </table>
{{else}}
  <div class="sparql-history-no-queries">No SPARQL query has been run yet.</div>
{{/if}}